import { defineStore } from 'pinia';
import StoreNames from '@/stores/StoreNames';

export const useExtrasStore = defineStore(StoreNames.EXTRAS_STORE, {
    state: () => ({}),
    getters: {},
    actions: {},
  });